doctype html
html(lang='en')
  +norska_head
  body.bg-theme
    include ../header.pug
    .mt-9.p-1
      block content
      #empty.hidden.p-5.text-6.white.text-center Sorry, no map found
      #hits.js-masonryWall.min-h-100vh
      !=include("_includes/templates/footer.pug")

    textarea#hitTemplate(hidden)
      .js-masonryBrick.relative.bg-gray-1.rounded.shadow-2.border-001.border-gray-4.transform.transition-transform.hover_scale-105p
        .js-masonryContent(id="{{objectID}}")
          !=include("_includes/templates/hit.pug")

  script!=`window.CONFIG = ${JSON.stringify(data.config)}`
  +norska_scripts
